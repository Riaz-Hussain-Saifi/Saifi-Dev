import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Saifi Developer - Full Stack Developer & AI Specialist",
  description: "Professional portfolio of Riaz Hussain Saifi, a Full Stack Developer specializing in Next.js, React, Python, AI and more.",
  keywords: 'web developer, full stack developer, react developer, next.js developer, python developer, ai, machine learning',
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
